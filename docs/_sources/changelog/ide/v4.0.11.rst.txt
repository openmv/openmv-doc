.. _changelog_ide_v4_0_11:

v4.0.11
=======

v4.0.11 is a maintenance release that hardens the firmware-flashing tools
against concurrent operations and repairs the Windows installer's bundled USB
driver installation. It stays on Qt Creator 10.0.1, and there are no breaking
changes.

.. contents:: On this page
   :local:
   :depth: 1

Highlights
----------

- **Thread-safe flashing tools:** bootloader, DFU, and firmware-download
  operations are now guarded by real mutex locks, preventing race conditions
  when triggered concurrently.
- **Windows USB drivers restored:** Arduino board drivers (GIGA, Nicla Vision,
  Portenta H7, Nano RP2040 Connect, Mimsy) are re-enabled and the FTDI/Arduino
  driver installer exit-code handling is fixed.

Bug fixes
---------

- The firmware-flashing tools (``bossac``, ``dfu-util``, ``imx``,
  ``picotool``, and ``myqserialportinfo``) now guard their work with real
  ``QMutex`` locks instead of plain busy-flag booleans, preventing race
  conditions when bootloader, DFU, firmware-download, and device-enumeration
  operations are triggered concurrently
  (`565576085 <https://github.com/openmv/qt-creator/commit/565576085>`__).

Platform and tool support
-------------------------

- **Qt Creator base:** 10.0.1.
- The Windows installer's bundled USB driver installation was fixed: Arduino
  board drivers (GIGA, Nicla Vision, Portenta H7, Nano RP2040 Connect, Mimsy)
  are re-added and re-enabled, and the FTDI and Arduino driver commands now
  accept the correct installer exit codes
  (`4a3327ad0 <https://github.com/openmv/qt-creator/commit/4a3327ad0>`__,
  `0de03a33a <https://github.com/openmv/qt-creator/commit/0de03a33a>`__,
  `fa4914806 <https://github.com/openmv/qt-creator/commit/fa4914806>`__).

Breaking changes
----------------

None. Existing projects and workflows continue to work unchanged.
