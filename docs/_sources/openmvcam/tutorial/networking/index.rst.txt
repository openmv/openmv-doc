Networking
==========

This super-section is **planned** -- pages will land in a
later session.

Once the application can run concurrently, the next layer
is talking to the rest of the network. This section covers
the :mod:`network` module, the camera's wireless interfaces,
TCP and UDP sockets, DNS, and the patterns for building
network clients and small servers on top of them.

(Placeholder.)
